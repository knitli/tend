//! agentui run — CLI wrapper entry point.
//!
//! Phase 2 scaffold only. The full implementation of args parsing, PTY
//! spawning, and daemon-IPC talk lands in US1 (T054–T058).

fn main() {
    eprintln!("agentui run: not implemented yet (see tasks T054–T058)");
    std::process::exit(2);
}
