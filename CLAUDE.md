# tend Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-11

## Active Technologies

- Rust stable ≥ 1.80 (backend + CLI); TypeScript 5.x + Svelte 5 runes (frontend) + Tauri 2.x, `portable-pty`, `sqlx` (sqlite feature), `tokio`, `notify`, `sysinfo`, `tracing`, `serde` / `serde_json`; xterm.js 5 with `xterm-addon-fit` and `xterm-addon-web-links`, `marked`, `DOMPurify`, Vite (001-cross-repo-workbench)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test && cargo clippy

## Code Style

Rust stable ≥ 1.80 (backend + CLI); TypeScript 5.x + Svelte 5 runes (frontend): Follow standard conventions

## Recent Changes

- 001-cross-repo-workbench: Added Rust stable ≥ 1.80 (backend + CLI); TypeScript 5.x + Svelte 5 runes (frontend) + Tauri 2.x, `portable-pty`, `sqlx` (sqlite feature), `tokio`, `notify`, `sysinfo`, `tracing`, `serde` / `serde_json`; xterm.js 5 with `xterm-addon-fit` and `xterm-addon-web-links`, `marked`, `DOMPurify`, Vite

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
